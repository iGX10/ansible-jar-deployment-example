deployservers:
  hosts:
    deploy_server1:
      ansible_host: "20.86.74.48"
      ansible_user: gx
      ansible_password: "{{ vaul_remote_host_password }}"
      ansible_become: yes
      ansible_become_password: "{{ vaul_remote_host_password }}"
